@model IEnumerable<Deportivo.Web.Data.Entities.Accesorios>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.acce_desc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.acce_obs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.acce_act)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.acce_fecing)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.acce_desc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.acce_obs)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.acce_act)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.acce_fecing)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_acce">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_acce">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_acce">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
