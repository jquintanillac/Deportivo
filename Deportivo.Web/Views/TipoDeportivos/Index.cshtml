@model IEnumerable<Deportivo.Web.Data.Entities.TipoDeportivo>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tipdep_desc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tipdep_act)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tipdep_desc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tipdep_act)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_tipdep">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_tipdep">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_tipdep">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
